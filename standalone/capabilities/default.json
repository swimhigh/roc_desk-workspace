{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the coding workspace standalone window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "opener:default",
    "opener:allow-open-path",
    "dialog:default",
    "dialog:allow-open"
  ]
}
